import React from 'react';

const Form = () => (
  <form id="form">
    <input
      type="text"
      placeholder="Saisissez votre message ..."
    />
    <button type="submit">&gt;</button>
  </form>
);

export default Form;
